name: Generate profile summary cards

on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  cards:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Generate summary cards into the repo
        uses: vn7n24fzkq/github-profile-summary-cards@release
        env:
          # PERSONAL_ACCESS_TOKEN (classic PAT, repo + read:user scopes) unlocks
          # private commits/issues/contributions; falls back to the default
          # public-only token until that secret is configured.
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
        with:
          USERNAME: ${{ github.repository_owner }}
          BRANCH_NAME: main
          UTC_OFFSET: -5
